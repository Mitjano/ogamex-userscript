# HANDOFF 14.09.2026 — pełny stan po dwóch dobach obrony floty

**Czytaj to PIERWSZE.** Dokument dla sesji na laptopie Windows. Pamięć Claude Code jest lokalna
dla maszyny, więc wszystko, co trzeba przenieść, jest tutaj.

Aktywny plik: `ogamex-3.user.js` (Genesis). Push na `main` = auto-deploy do Tampermonkeya.
**Bump `// @version` ORAZ `const VERSION` przy KAŻDEJ zmianie** — test tego pilnuje.

---

## 1. Stan na teraz

| co | wartość |
|---|---|
| wersja w repo | **3.96.0** (sesja popołudniowa 14.09 na Windows — patrz sekcja 8) |
| właściciel | Genesis, baza ekspedycyjna **[3:279:1]** („Colony 66"), 23 kolonie |
| sąsiedzi | agresywni, atakują z **[3:280:1]** — DESTROY na księżyce, ACS, skany całego konta |
| kanał alarmowy | ntfy `ogx-4wrgtgf1zknuoa`, przypięty w kodzie, wspólny ze strażnikiem |
| pakiet testów | `node test3-all.js` — macierz 672, war-game, decyzyjne, 341 E2E, panel, zegar |

### Środowisko na Windows (to się zmienia względem Maca)
- Gra chodzi w **Chrome + Tampermonkey**. Wymagane „Zezwalaj na skrypty użytkownika"
  (`chrome://extensions` → Szczegóły).
- **Magazyn ustawień jest PER PRZEGLĄDARKA i PER MASZYNA.** Na nowym laptopie bot wstanie
  z pustym `cfg`, czyli `autoRescue:false` (tryb OBSERWATOR — widzi atak i NIE rusza flotą),
  a panel i tak pokaże „ON". **Pierwsza rzecz po instalacji: sprawdzić auto-ratunek, ciało
  startowe ekspedycji, rezerwę deuteru i prędkość ucieczki (3%).**
- **Odczyt stanu bota z dysku** (bezcenny przy diagnozie): klucze `genesis.ogamex.net:ogx3_*`
  leżą czystym tekstem w LevelDB Tampermonkeya.
  - macOS: `~/Library/Application Support/Google/Chrome/Default/Local Extension Settings/dhdgffkkebhmkfjojejmpbldmpobfkfo/*.log`
  - **Windows: `%LOCALAPPDATA%\Google\Chrome\User Data\Default\Local Extension Settings\dhdgffkkebhmkfjojejmpbldmpobfkfo\*.log`**
  - Metoda: skopiuj `*.log` i `*.ldb` do katalogu tymczasowego i puść regex po
    `"genesis.ogamex.net:ogx3_<klucz>":"s<JSON>"`. Wartość ma jednoznakowy prefiks typu.
  - **Klucz `ogx3_log` (400 linii logu) NIE parsuje się jako całość** — LevelDB tnie duże
    wartości na bloki i wstawia w środek 7-bajtowe nagłówki. Wyciągaj wpisy regexem
    `\{\\"time\\":\\"(\d\d:\d\d:\d\d)\\",\\"msg\\":\\"(.*?)\\",\\"type\\":\\"(\w+)\\"\}`.
  - Najważniejsze klucze: `situation` (hangary, pary, zagrożenia, loty, moonLost),
    `journal` (dziennik obrony — to, co idzie na telefon), `once` (kiedy padł który komunikat,
    świetne do rekonstrukcji), `cfg`, `bonus`, `impacts`, `ntfy_last`, `ver`, `last_tick`.

---

## 2. Co się wydarzyło — dwa incydenty, które ukształtowały tę sesję

### (a) 13.09 04:29–05:44 — bot NIE obronił wracających fal
Bot ewakuował księżyc o 04:30 i **przez 68 minut nie wysłał ani jednego ratunku**, mimo że
wracały fale ekspedycji po 115–164 mln statków. Właściciel uratował **~1,64 mld statków**
ręcznym Fleet Save o 05:44. O 05:40 księżyc padł.

Przyczyna: decyzja szła po migawce hangaru sprzed ewakuacji („43 Gwiazdy Śmierci"), a o tym,
że wylądowała fala, bot wiedział z **innego źródła** (rejestr powrotów) i używał tej wiedzy
WYŁĄCZNIE do pisania komunikatów. Jeden przebieg `decide()`, dwa sprzeczne obrazy świata.

Ta noc jest teraz **stałym testem** (blok 77 w `test3-decide.js`): na wersji sprzed poprawki
0/6 ratunków, na dzisiejszej **6/6**, plus push w każdej z sześciu chwil.

### (b) 14.09 03:2x — nalot na trzy księżyce naraz
DESTROY na [3:279:1], potem na [3:279:11] i [3:279:12]. **Obrona zadziałała wzorowo** — każda
wracająca fala dostała własny ratunek, właściciel to potwierdził. Wyszły za to trzy inne rzeczy:
sondy udające nadwyżkę na pasku, odbudowa księżyca zagłodzona przez alarm i bot wyrywający
operatorowi stronę. Wszystkie naprawione (v3.95.0–3.95.3).

---

## 3. Co poszło na produkcję w tej sesji (v3.86 → v3.95.3)

| wersja | rzecz |
|---|---|
| 3.86.0 | pasek misji odświeżany cichym fetchem — bezczynny bot przestał ślepnąć w nocy |
| 3.87.0 | cisza listy przy bazie ekspedycyjnej budzi właściciela (pierwszy alarm z flagą `push`) |
| 3.88.0 | przerwa „rytm człowieka" nie blokuje bonusu, gdy właściciel klika po grze |
| 3.89.0 | własne przeładowanie strony ≠ awaria GŁÓWNEGO detektora ataków |
| 3.90.0 | „Księżyce OFF" zatrzymuje nowe, ale **nie** odbudowę zniszczonego |
| **3.91.0** | **P0**: przestarzały hangar nie unieważnia ratunku wracających fal |
| **3.92.0** | **P0**: odmowa ratunku wymaga świeżego dowodu (`hangarPewny`); własny log bota przestał parsować się jako pasek misji; atak bez czytelnego odliczania przestał znikać |
| 3.93.0 | macierz bojowa 672 scenariuszy + ratunek z rejestru, gdy migawka pusta |
| 3.94.0 | skan przestał przychodzić na telefon jako „⚔️ ATAK" |
| 3.95.0 | sonda bez licznika przestała udawać nadwyżkę; odbudowa wyjęta z kolejki ekonomii |
| 3.95.1 | odbudowa idzie mimo trwających ataków; własny limit prób (20 zamiast 3, karencja 60 s) |
| 3.95.2 | pod ostrzałem tylko odbudowa (nie nowe księżyce); utrata księżyca widziana w tle (~100 s zamiast 10 min) |
| 3.95.3 | rekonesans przy alarmie nie wyrywa strony, gdy operator gra |

---

## 4. Czym to jest pilnowane (NIE PSUĆ)

Trzy automaty chodzą przy każdym `node test3-all.js`:

1. **`test3-matrix.js` — macierz bojowa, 672 scenariusze.** Kombinacje: typ ataku × czas dolotu
   × stan pamięci o hangarze × lot w powietrzu × dostępne refugium. Niezależny **sędzia** zna
   prawdę scenariusza (ile floty naprawdę stoi) i pyta: „skoro tam stoi flota, jest czas i jest
   dokąd uciec, to czy bot ruszył?". Wynik ma być **672/672**.
2. **Blok 77 w `test3-decide.js` — noc 13.09 odtworzona co do minuty** z prawdziwych danych.
   Ma dawać **6/6 ratunków i 6/6 pushy**.
3. **E2E 60b i 43b/43c** — fala wracająca pod uderzenie, odbudowa przy wyłączonym module,
   odbudowa pod ostrzałem.

**Właściciel poprosił wprost: „pilnuj, żeby przy update tego nie popsuć".** Traktuj te trzy
jako kontrakt.

---

## 5. Zasady pracy, które w tej sesji kosztowały najwięcej

- **Test mutacyjny jest obowiązkowy i trzeba sprawdzić, czy mutacja weszła.** Dwa razy mutacja
  „przeszła", bo w ogóle się nie zapisała (`assert` padł, a skrypt i tak poleciał). Po wstawieniu
  mutacji zawsze `grep` w pliku.
- **E2E bywa za miękkie na defekty decyzyjne.** Scenariusz 60b przechodził z WYŁĄCZONĄ poprawką,
  bo atrapa oddaje świeży hangar zbyt chętnie. Gdy poprawka dotyczy czystej decyzji, dowód ma
  być w `test3-decide.js`.
- **Zawężaj do SYGNAŁÓW ZMIANY, nie do „braku danych".** Pierwsza wersja `hangarPewny` alarmowała
  przy każdym pustym księżycu i zepsuła osiem zachowań. Różnica między naprawą a nową regresją.
- **Alarm, który kłamie, jest gorszy niż brak alarmu.** Cztery razy w tej sesji: „wysyłam DRUGI
  lot" bez lotu, „GŁÓWNY detektor nie widzi NIC" po własnym przeładowaniu, „⚔️ ATAK" na skan,
  „nie ma czego ratować" nad pełnym hangarem. Tym samym kanałem idzie prawdziwy alarm.
- **Polski cudzysłów „ zamknięty zwykłym `"` wysadza plik JS.** Znowu.
- **Blok dopisany na końcu `test3-decide.js` nie wykona się** — `process.exit` jest przed nim.
  Wstawiaj przed linią z podsumowaniem.
- Sprawdzaj `echo $?` bez pipe'a — pipe zjada kod wyjścia.

---

## 6. OTWARTE — do zrobienia

### Z audytu warstwy wykonawczej — **ZAMKNIĘTE w v3.96.0 (14.09 popołudnie, sekcja 8)**
1. ~~Odmowa gry po „Send fleet" księgowana jako udana wysyłka~~ → bramka anty-duplikat żąda dowodu
   z hangaru (`Fly.sendProof`), E2E 66.
2. ~~`extend` przesuwa zawrót tylko JEDNEMU lotowi z pary~~ → `extendAll` w decide + egzekutor po `id`,
   decide 81, E2E 64.
3. ~~Nieczytelna strona floty = „hangar pusty"~~ → ponowienie z sufitem 3 i głośne przerwanie, E2E 67.
4. ~~Przy nalocie na 2+ par druga czeka cały przebieg~~ → cichy odczyt dla każdej pary w przebiegu
   (sufit 3), `continue` po nieudanym starcie, uczciwy komunikat „W KOLEJCE", E2E 65.

### Z audytu odbudowy księżyca — patrz `AUDYT-ODBUDOWY-2026-09-14.md`
**Cztery P0 zamknięte w v3.96.0** (licznik prób kasowany po sukcesie, odrzucony submit „Form a moon"
rozpoznawany, potwierdzenie odbudowy i zwóz floty PRZED bramką ataków, `Moon.pending()` u wołającego;
E2E 68, decide 82). **Pięć P1 i P2 nadal otwarte** (przełączenie planety pod odbudowę przestawia
detektor ataków, `planet_drift`, martwy klucz w `s.pairs`, priorytet utraconego księżyca w `target()`,
flota ewakuowana PRZED odbudową nie wraca).

### Sprawy właściciela
- **Rozważa przeniesienie bazy ekspedycyjnej do galaktyki 1** — najlepszy układ to **1:217**
  (cztery księżyce w jednym układzie, czyli trzy schrony dla ratunku). Zmiana to jedno pole
  w panelu: Ustawienia → Ekspedycje → „startuj z". UWAGA (14.09 popołudnie): pasek planet na
  laptopie pokazuje kolonie **[4:22:3] i [4:22:4], obie z księżycami** (hangary nigdy nie czytane) —
  zdanie „w galaktyce 4 nie ma żadnej kolonii" jest nieaktualne; wyjaśnić przed decyzją.
- Fleet Save jest **wyłączony** (świadomie albo nie — warto potwierdzić).
- Duplikat `id="ogx3-quiet"` w panelu: „Tryb cichy" jest nieosiągalny. Poprawka była napisana
  i cofnięta na prośbę właściciela — nie dotyka obrony.

---

## 7. Doktryna obrony (obowiązuje, nie zmieniać bez rozmowy)

Pełna w `DOKTRYNA-DESTROY-2026-09-11.md`. Skrót:
- Ucieczka: sąsiedni księżyc w układzie → przy DESTROY bez wolnego księżyca **najbliższa spokojna
  kolonia** (nigdy planeta pod ginącym księżycem) → drugie ciało pary → refugium.
- Misja **stacjonuj**, prędkość **3%** — flota ma WISIEĆ w locie, żeby dało się ją zawrócić;
  zawrócony stacjonuj nie jest widoczny na falandze.
- **Każda wracająca fala dostaje własny ratunek** — to jest rzecz, którą właściciel pochwalił
  i której pilnują testy 77 i 60b.
- **Gwiazdy Śmierci zostają w domu** jako hamulec dla kolejnych fal (decyzja właściciela 11.09).
- Poniżej **40 s** do uderzenia bot nie zdąży z formularzem — zostaje alarm z pushem.
- Obrona ma **bezwzględne** pierwszeństwo nad ekonomią i nad odbudową księżyca.

---

## 8. Sesja popołudniowa 14.09 (laptop Windows) — v3.96.0

### Co zastałem na laptopie (odczyt z LevelDB, bez dotykania gry)
- `cfg.enabled:false` przy `autoRescue:true` — klik „Bot OFF" w panelu o **08:32:25**, 30 s po
  otwarciu karty: bot v3.85.0 wstał po 64 h ciszy, w 11 s postawił księżyc przy [2:227:5], wysłał
  falę ekspedycji i dwa razy przestawił stronę; Tampermonkey w tym czasie podmienił skrypt na
  3.95.3. **`enabled:false` to nie obserwator — to zero detekcji, alarmów i pushy.** Resztę
  ustawień (3 %, deuter 5 mld, expo z [3:279:1], recon ON, FS OFF) miał dobrze. Włącza właściciel.
- `human.ecoIdleSec` = 0: ekonomia nie czeka, aż operator przestanie klikać. Na laptopie, gdzie
  właściciel gra ręcznie, pole „Ekonomia czeka N min" w panelu jest właściwą dźwignią.
- Na Windows **nie ma strażnika** (`watchdog/` = macOS; `hb_ok:false`, `hb_ever` brak).
- LevelDB: wartości dłuższe niż reszta bloku są cięte 7-bajtowym nagłówkiem na KAŻDEJ granicy
  32 768 B pliku `.log` — po wycięciu tych bajtów `situation` i `ogx3_log` parsują się w całości
  (poprawka do sekcji 1).

### Testy na tej maszynie
Pełne E2E było czerwone o **zmiennym** zbiorze scenariuszy (41/47/57; 8/57; na v3.95.1: 34/39/47/57).
Przyczyna: harness czeka stałe 140 ms na osadzenie się asynchronicznej pracy bota, laptop jest
wolniejszy od Maca. `OGX_SETTLE_MS=400 node test3-all.js` → ten sam kod 341/341. Knob w harnessie
(domyślnie 140, Mac bez zmian). **Czerwone E2E o zmiennym zbiorze = najpierw okno, potem regresja.**

### Co poszło w v3.96.0 (jeden push, po pełnym pakiecie i teście mutacyjnym)
| # | zmiana | dowód |
|---|---|---|
| 0 | `OGX_SETTLE_MS` w `test3-e2e.js` | 4 przebiegi kontrolne |
| 1 | `extendAll` w decide: dosłana fala przesuwa zawrót KAŻDEGO lotu pary; egzekutor dopasowuje po `id` | decide 81a–h, E2E 64 |
| 2 | cichy odczyt hangaru przy alarmie dla każdej pary w przebiegu (sufit 3, `continue` na dławiku); `continue` po nieudanym `Fly.start`; dziennik mówi „W KOLEJCE zaraz po tej wysyłce", nie „BEZ RATUNKU" | E2E 65 (zachowanie), decide 82f (źródło) — **uczciwie: 65 nie pada na mutacji `continue → break`**, atrapa nie umie utrzymać ciepłego dławika pary A przy jej jednoczesnej prośbie; tę mutację łapie tylko 82f |
| 3 | bramka anty-duplikat dla lotów obronnych żąda DOWODU z hangaru (`Fly.sendProof`, stempel niesie `before`): zmalał → księguje; stoi → odmowa (ponowienie, druga = przerwanie z BŁĄD); nieczytelny → migawka NIETKNIĘTA, misja bez karencji, decide ponawia | E2E 66a–o |
| 4 | `Fly.form`: strona nieczytelna ≠ pusty hangar (ponowienie, sufit 3, głośne przerwanie); „formularz w toku" (krok 2/3) = jeden przebieg czekam, potem przeładowanie formularza od kroku 1, czwarty przebieg = głośne przerwanie | E2E 67, 18, 44, 57, 60 |
| 5 | odbudowa: weryfikacja PRZED bramką ataków, sukces kasuje `tries`, odrzucony „Form a moon" rozpoznany (tekst strony → log, BŁĄD przy odbudowie), zwóz odłożony (`st.zwoz`) do ciszy, `Moon.pending()` u wołającego | E2E 68a–m, decide 82 |

### Lekcje harnessu E2E z tej sesji (kosztowały najwięcej)
- **Krok formularza atrapy przeżywa przeładowanie, a osierocony start `defenceTick` z poprzedniego
  okna wywołuje `form()` drugi raz w środku kroku 2.** Scenariusze 18 i 44 przechodziły dzięki
  drodze „hangar pusty → cicho skasuj misję (bez karencji)". Po jej zamknięciu potrzebne były:
  rozróżnienie „formularz w toku" od „strona nieczytelna" oraz `advance()` przewijające `fly_block`
  (18 deklarował „45 s", a nigdy tego nie mierzył).
- **Wiersz atrapy pokazuje odliczanie „05:00", a parser bierze tekst, nie atrybut `eta`** —
  uderzenie w E2E to zawsze ~300 s, niezależnie od `eta` w scenariuszu.
- **`hangarPewny`: brak migawki = pewność** („flota wyleciała, nie ma czego ratować" = cisza).
  Nigdy nie „zapominaj" hangaru jako odpowiedzi na brak dowodu; zostaw migawkę i pozwól decide
  ponowić — formularz czyta hangar na żywo. (Zasada z sekcji 5: sygnały zmiany, nie brak danych.)
- **Cichy rekonesans przy alarmie istnieje tylko w gałęzi „z pary trwa lot"**; test bez lotu
  w powietrzu dostaje rekonesans nawigujący i inne komunikaty.
- Chirurgia stanu + `Moon.tick` pod ostrzałem: atak w stanie żyje do `arriveAt` — koniec nalotu
  w teście to `threats.length = 0` **i** `advance()`.
- Mini-harness jednego scenariusza (nagłówek pliku + helpery + jeden blok + stopka) skraca pętlę
  diagnozy z 8 min do 1 min; budowany `sed`-em w scratchpadzie, patchowany node-em (nie heredoc —
  Git Bash na Windows zjada backslashe w heredocach).

### OTWARTE po tej sesji
- P1/P2 odbudowy z `AUDYT-ODBUDOWY-2026-09-14.md` (przełączenie planety pod odbudowę przestawia
  detektor ataków; `planet_drift`; ~~martwy klucz w `s.pairs`~~ **ZAMKNIĘTE v3.96.1 (sekcja 9)**; priorytet utraconego księżyca; flota
  ewakuowana PRZED odbudową nie wraca).
- Strażnik na Windows (Task Scheduler + python) — nie zaczęty.
- Galaktyka 4 (pkt „Sprawy właściciela"), przeniesienie bazy do 1:217, FS OFF, duplikat `ogx3-quiet`.
- Do potwierdzenia NA ŻYWO przy pierwszej okazji: co fork pisze przy odrzuconym „Form a moon"
  (tekst strony idzie teraz do logu) i czy po odrzuconym „Send fleet" oddaje /fleet z pełnym
  hangarem (bramka liczy z hangaru, nie z tekstu — działa niezależnie od markupu).

---

## 9. Sesja wieczorna 14.09 (Mac) — v3.96.1: przenosiny planety = duch pary

**Co się stało (21:36–21:43):** owner przeniósł bazę [2:224:7] (planeta + księżyc, UUID fedb6673…)
na [2:184:1] i ustawił tam start ekspedycji oraz stały cel FS. Bot co przebieg wystawiał Fleet Save
z [2:224:7] moon (hangar 3,29 mld — największy, więc wygrywał sortowanie), Fly kończył
„[LOT] przerwany: brak [2:224:7] moon na pasku planet", karencja trasy i tak w kółko; push
„⚠️ Obrona: BŁĄD" o 21:42. Do tego „FS: skonfigurowany cel [2:184:1] to ta sama para" — cel FS
wskazywał parę, na której flota teraz stoi.

**Przyczyna:** `s.pairs` tylko rosło. `Situation.refresh()` dopisywał pary z żywego paska planet,
ale nic nie kasowało klucza, który z paska zniknął (ten sam „martwy klucz w `s.pairs`" z audytu
odbudowy). Hangar `2:224:7|moon` żył pod duchem do 48 h.

**Poprawka (v3.96.1):** w `refresh()`, PO scaleniu z drugą kartą (jak v3.68.10): para nieobecna
na ŻYWYM pasku (strona z ≥ 1 parą) w dwóch odczytach odległych o ≥ 60 s (`s.pairGone[k]`) znika
ze stanu razem z hangarami obu ciał, `moonLost`, `rescues`, `landings` i zmierzonymi czasami FS;
log `[PASEK] para [k] zniknęła z paska planet…` + wpis dziennika rodzaju „STAN" (bez pusha).
Pasek z fetcha /home NIE kasuje par (bywa niepełny).

**Dowody:** decide 83a–e (źródło), E2E 69a–i (odtworzony incydent: baza 700 BS → po przenosinach
600 BS, duch z WIĘKSZYM hangarem). Test mutacyjny (`if (false && livePairs.length)`, grep potwierdził
wejście): 69b/d/e/g/h/i czerwone, `g.sent = []` — dokładnie stan z 21:43. Pełny pakiet: `node test3-all.js` 14.09 ~22:20 na Macu — WSZYSTKO PRZESZŁO (decide + macierz 672 + war-game + E2E 396 sprawdzeń + panel + zegar + składnia), exit 0.

**Dla ownera po KAŻDYCH przenosinach (kod tego nie robi):** stały cel FS ≠ para z flotą; start
ekspedycji; lista księżyców w konfiguracji Moon (log wciąż mówi „nie widzę [2:287:5] na pasku planet").

**Testy na Macu:** jsdom leży w `~/node_modules` (nie w repo) — `node test3-all.js` z katalogu repo
działa; mini-harness jednego scenariusza w scratchpadzie wymaga `NODE_PATH=/Users/mch/node_modules`.

---

## 10. Noc 14/15.09 i poranek 15.09 (Mac) — v3.96.2: trzy poprawki po analizie nocy

**Skąd wiedza:** pełny log i dziennik odczytane z dysku — Tampermonkey w Chrome trzyma GM storage
w LevelDB (`Local Extension Settings/dhdgffkkebhmkfjojejmpbldmpobfkfo`, klucz `!extdb.@st#<uuid>`,
trzy warstwy JSON + jednoliterowy prefiks typu). Narzędzie: `tools/mac_chrome_state.py [katalog]`
(kopia bazy → classic-level w Node → `gm_*.json`, `log_chrono.txt`, `journal_chrono.txt`).
Log bota to 400 wpisów, więc przy zalewie ostrzeżeń obejmuje < 2 h — noc dało się odtworzyć TYLKO
z dziennika (600 wpisów z datami). Zrzuty = dane z gry, nie commitować.

**Co się działo w nocy:**
- 22:41 FS z [5:238:12] (główna flota, 13 typów) + trzy floty ciężarówek na [5:238:11]; zawroty
  02:51–02:54, lądowanie 07:00–07:10. Dwa skany sond (22:33, 01:14) i trzeci 05:35 na [1:225:9]
  (planeta z ~379 tys. ciężkich transportowców widocznych dla falangi). Zero ataków.
- **Trasa galaktyka 1 → [5:238:11] padała całą noc:** [1:218:9], od 01:54 [1:217:6] (192 337 HC),
  „Next (krok 2) martwy" (przycisk WYŁĄCZONY przez 25 s). 3 próby co godzinę × 8 h = 27 przerwanych
  lotów, push „⚠️ BŁĄD" co godzinę. Zrzut „tekst formularza: …" był PUSTY (`#content` bez tekstu),
  więc powodu nie znamy; najpewniej deuter na księżycu źródła (lot 4 galaktyki, 11,6 h).
- **Fałszywy alarm:** od ~03:50 co 15 min „lot [5:238:12]→[5:238:11] dawno po terminie zawrotu —
  sprowadź flotę ręcznie" dla czterech ZAWRÓCONYCH lotów (`flightStale` = godzina po `recallAt`,
  bez rozróżnienia zawróconego od niezawróconego). Bez pusha, ale czerwone kłamstwo w panelu.
- **Zalew logu:** 20 par z transportowcami na planetach × ostrzeżenie co 10 min = ~120 linii/h.

**Poprawki (v3.96.2):**
| # | zmiana | dowód |
|---|---|---|
| 1 | `decide`: lot w fazie `recalled` po `recallAt`+1 h → warn „zawrócony o HH:MM, wraca — ląduje ~HH:MM" (lądowanie = `recalledAt` + czas do zawrotu); dopiero > 1 h po lądowaniu bez domknięcia hangaru → error „powinien był wylądować… sprawdź ręcznie"; `recall_failed` bez zmian | decide 84a–d; mutacja `if (false && …)` → 84a/b/c czerwone |
| 2 | `Fly.clickWhenEnabled`: zrzut przy martwym Next = tekst strony wokół „Duration of flight" (±250/450 znaków) + osobna linia z elementami o paliwie/deuterze/slotach; `fs_try`: po 3 porażkach trasa odpoczywa **6 h** (było: do końca godziny), jedna linia „odpuszczam tę trasę na 6 h (do HH:MM)"; udana wysyłka nadal zeruje licznik | E2E 70a–f (atrapa dostała `disabledNext` = przycisk jest, ale wyłączony — dokładnie jak w nocy); 70c woła `clickWhenEnabled` wprost, bo w harnessie kolejny przebieg przerywa czekanie (formularz „w toku" → przeładowanie); mutacja 6 h→1 h → 70e/70f czerwone; decide 84g–h (źródło) |
| 3 | `decide`: pary z flotą tylko na planecie zbierane do `fsNaPlanecie` → JEDNA linia na godzinę (klucz `fs-planeta`, liczba par, suma, lista od największej, tekst nadal zawiera „nie wysyłam stamtąd (falanga)") | decide 84e–f; mutacja (per-para z powrotem) → 84e/f czerwone |

Pełny pakiet: `node test3-all.js` 15.09 ~07:40 na Macu — WSZYSTKO PRZESZŁO (decide + macierz 672 + war-game + E2E 402 sprawdzeń + panel + zegar + składnia), exit 0.

**Dla ownera:** deuter na księżycu [1:217:6] (albo bliższy cel FS dla galaktyki 1); transportowce na
20 planetach są widoczne dla falangi i sond — bot ich nie ruszy; po lądowaniu głównej floty
(07:10) FS był OFF (wyłączony ręcznie 06:51), ekspedycje ON z [5:238:12].

**Lekcja harnessu:** „przycisk jest, ale WYŁĄCZONY" (`disabledNext`) i „przycisku NIE MA" (`deadNext`)
to dwie różne ścieżki bota; ta druga od v3.96.0 kończy się jako „formularz w toku" i nigdy nie
dochodzi do 25-sekundowego czekania ani zrzutu. Scenariusz nocy wymaga tej pierwszej.

---

## 11. 15.09 rano — v3.96.3: FS na 3%

Owner: „wysyłaj FS na 3% — mniej deuteru, flota i tak zawracana". Blokada siedziała TYLKO
w polu panelu (zaokrąglanie do dziesiątek z v3.68.1, pisane zanim Fly nauczył się czytać
realną listę prędkości forka). Pole przyjmuje teraz wartości z listy Genesis
(3, 5, 10…100 — zrzut 11.09), wpis przycinany w dół jak w Fly („najbliższa nie szybsza").
Dowód: test3-ui (3→3, 7→5, 17→10, 1→3, 100→100). Po zmianie prędkości pierwszy lot na
trasie mierzy czas od nowa (fsMeasured kluczowane prędkością) — to oczekiwane.
OTWARTE (propozycja, owner jeszcze nie zatwierdził): FS dosyłowy — fala lądująca w trakcie
lotu FS dostaje własny lot na ten sam cel (dziś gate `!f` = jeden lot FS na parę w powietrzu;
reszta fal nocuje na księżycu). Wymaga rozróżniania wierszy tej samej trasy na liście ruchów
(zapamiętany data-fleet-id przy wysyłce + rezerwowo dopasowanie po godzinie dolotu).

---

## 12. 15.09 — v3.97.0: FS DOSYŁOWY (owner zatwierdził)

Owner: „przy 10 falach wracających z ekspedycji, jak ustawię powrót FS na 5:20, bot po każdym
powrocie wyśle flotę na FS i zawróci każdą tak, żeby wróciła o 5:20?" — TAK, po tej zmianie.

**Co było:** bramka `!f` w decide = jeden lot FS na parę w powietrzu. Fale lądujące w trakcie
lotu FS nocowały na księżycu do rana (a ekonomia stała, bo pauza „flota na Fleet Save" jest
globalna). Wczoraj zadziałało tylko dlatego, że ostatnia fala ekspedycji wróciła PRZED startem FS.

**Co jest (v3.97.0):**
- decide — nowy warunek `fsDosylowy`: fala świeżo na księżycu (`fleet.body==="moon"`, `fleet.at`
  NOWSZY niż ostatnia wysyłka FS z tej pary) + wszystkie wiszące loty pary to FS → dopuszcza
  DRUGI (trzeci…) lot FS z tej samej pary. Bramka: `if ((!f || fsDosylowy) && …moon)`. Zawrót
  liczy się per lot (arytmetyka „połowa drogi" od `sentAt`), więc wszystkie wracają o 5:20.
  `why` niesie „DOSYŁOWY", żeby operator w logu odróżnił to od duplikatu.
- zawrót — przy KILKU lotach tą samą trasą wiersze listy ruchów są nieodróżnialne po treści
  (lekcja v3.76.0). Rozpoznanie PO DOLOCIE: nasz wiersz to ten, którego licznik jest najbliżej
  `(sentAt+flightMs)−teraz`. Jeden kandydat = po staremu.

**Bezpieczniki bez zmian:** rezerwa slotów (przy NIEZNANYCH slotach dosyłowego NIE ma — `wolne =
fsAir ? 0 : 1`), pierwszeństwo ratunku, bramka dowodu z hangaru przeciw duplikatom, „hangar
świeższy niż wysyłka" odsiewa ducha floty, która właśnie wyleciała.

**Dowody:** decide 85a–g, E2E 71a–g. Dwie mutacje potwierdzone: `fsDosylowy=false` → 71b/e
czerwone; wybór wiersza „zawsze pierwszy" → 71e czerwone (to dokładnie ryzyko, o które owner
pytał: zawrócenie CUDZEJ floty). Pełny pakiet: `node test3-all.js` 15.09 na Macu — WSZYSTKO PRZESZŁO (decide + macierz 672 + war-game + E2E 409 sprawdzeń + panel + zegar + składnia), exit 0.

**Do potwierdzenia NA ŻYWO przy pierwszej wielofalowej nocy:** czy dopasowanie wiersza po dolocie
trafia, gdy 10 lotów tej samej trasy różni się startami o minuty (liczniki na liście ruchów
powinny się różnić o tyle samo). Gdyby fork podawał liczniki zbyt zgrubnie, plan B to zapamiętanie
`data-fleet-id` przy wysyłce — nie wdrożone teraz, bo dopasowanie po dolocie wystarcza i nie
zależy od stabilności id.

---

## 13. Noc 15/16.09 i poranek — v3.97.1: doba w czasie lotu, duch lotu, uczciwy alarm

Pierwsza noc z FS na 3 % i z v3.97.0. Zero ataków, flota cała. Trzy rzeczy do naprawy, wszystkie
znalezione z logu i dziennika (`tools/mac_chrome_state.py`).

**(1) P0 — zawrót o złej porze przy locie DŁUŻSZYM NIŻ DOBA.** Przy 3 % loty przez dwie i trzy
galaktyki trwają ponad 24 h, a fork pisze wtedy `Duration of flight (one way): 1d 08:41:48`.
Wzorzec znał tylko format godzinowy → `flightMs` zostawało 0 → blok „połowa drogi" w `Fly.form`
w ogóle się nie wykonywał → na wpisie zostawała GODZINA POWROTU. Cztery loty
([1:217:6], [1:218:9], [2:184:1], [2:220:7]) zawrócone o 05:00 zamiast ~01:48 wracały 08:20–11:28,
czyli sześć godzin po terminie. W stanie widać je po `flightMs: 0`; loty 19,64 h i 23,43 h miały
zawrót policzony poprawnie. Fix: opcjonalna grupa doby w wzorcu; gdy doba jest, reszta to zawsze
H:M:S (bez niej „12:30" nadal znaczy 12 min 30 s).

**(2) P0 — DUCH LOTU blokował ekonomię.** Właściciel ściągnął flotę FS ręcznie (06:03), a potem
przerzucił ją bramą skoków [4:22:3] → [2:184:1] (06:42). Wpis został „launched", bo pełny hangar
źródła bot tłumaczy jako powroty ekspedycji (reguła z testu 56a, słusznie). Skutek: `economyAllowed`
zwracało „flota jest na Fleet Save" MIMO FS OFF i ekspedycje stały godzinami. Fix dwuczęściowy:
przy potwierdzeniu wysyłki wpis dostaje `sentTotal` (ile statków naprawdę poleciało), a `flightAlive`
domyka wpis, gdy w hangarze źródła stoi CAŁA wysłana flota (fala ekspedycji jest ułamkiem, FS zabiera
wszystko). Wpisy bez `sentTotal` zachowują STARE zachowanie; jednego ducha sprzed wersji sprząta
jednorazowa migracja `migr_ghost_v3971` tuż przed filtrem `flightAlive`.

**(3) P1 — fałszywy alarm z v3.96.2.** Trzy loty wylądowały o 05:00 poprawnie, a o 06:01 dostały
czerwone „sprawdź ręcznie", bo alarm patrzył na sam zegar. Teraz: czerwono tylko wtedy, gdy hangar
źródła został odczytany PO terminie lądowania i jest pusty; wcześniej idzie cichy odczyt (`recon`
z decide) i zdanie o niewiedzy, nie o zaginionej flocie.

**Nie jest błędem:** ponowna wysyłka o 05:03. Flota wróciła 05:02, czyli po terminie 05:00, więc
`fsReturnAt` przeskoczył na jutro i FS wysłał ją na 23,43 h. Tak działa FS 3.x (flota w powietrzu
całą dobę, w domu tylko o godzinie powrotu). Jeśli właściciel chce mieć flotę w domu za dnia,
potrzebne jest OSOBNE okno ciszy po powrocie — do decyzji, NIE wdrożone.

**Dowody:** decide 84c (zaostrzony), 86a–g; E2E 72a–e (lot 33,3 h renderowany jako „1d 09:20:00"),
73a–e (duch domknięty, ekonomia rusza, fala ekspedycji wpisu NIE domyka). Mutacje: `dni = 0` →
72b/c czerwone; wzorzec bez grupy doby → 72b–e czerwone z `flightMs: 0` i zawrotem = godzina powrotu
(dokładnie nocny incydent); wyłączony dowód „cała flota wróciła" → 73b/c/d czerwone.

**Pełny pakiet:** `node test3-all.js` 16.09 na Macu — WSZYSTKO PRZESZŁO (decide + macierz 672 + war-game + E2E 419 sprawdzeń + panel + zegar + składnia), exit 0.

**Lekcja harnessu (kosztowała najwięcej):** atrapa gry ma DWA formattery czasu lotu — w `step2Html`
i w obsłudze kliknięcia prędkości. Bot czyta ten DRUGI. Dopóki był bez doby, scenariusz 72 przechodził
z wyłączoną poprawką. Złapała to dopiero mutacja.

---

## 14. 16.09 — v3.98.0: OKNO DNIA po powrocie FS

Owner: „co zrobić, żeby bot wysyłał flotę automatycznie po powrocie nocnego FS?". Do 3.97.1
odpowiedź brzmiała „nic się nie da" — FS 3.x trzyma flotę w powietrzu CAŁĄ DOBĘ (v3.68.0,
doktryna „flota w locie jest bezpieczniejsza niż w domu"), więc po powrocie o 05:02 wylatywała
z powrotem o 05:03:03 i ekonomia stała na „flota jest na Fleet Save" przez cały dzień. Jedyną
naturalną szparą jest okno startu `fsReturnAt − 2·czas lotu`, ale przy 3 % i lotach 19–23 h
jest ono zawsze otwarte (szpara istnieje tylko dla lotów KRÓTSZYCH niż pół doby).

**Nowa opcja `fs.restHours` (0 = zachowanie sprzed 3.98).** Przez N godzin po godzinie powrotu
FS NIE startuje: flota zostaje w domu i pracuje na ekspedycjach. Liczone z zegara, bez nowego
stanu — `fsRestUntil()` obok `fsReturnAt()` (poza decide, przez strefę czasową), wynik ląduje
w `s.fsRestUntil`. W decide okno zeruje `dest` w gałęzi FS, więc blokuje WYŁĄCZNIE nowe starty
Fleet Save. Zawroty lotów już wiszących, ratunek pod atakiem i ewakuacja z gołej planety
działają bez zmian (decide 87e pilnuje tego wprost). Panel: pole „po powrocie w domu N h",
pasek stanu pokazuje „okno dnia — flota w domu do HH:MM (ekspedycje), potem FS".

**Dla ownera:** przy powrocie 5:20 i `restHours` 16 flota pracuje 5:20–21:20, a potem wychodzi
na noc. Ustawienie to świadoma zmiana doktryny: w oknie dnia flota stoi w domu, więc chroni ją
sam ratunek (wykrycie ataku + ucieczka), nie „bycie w powietrzu".

**Dowody:** decide 87a–g, E2E 74a–d, panel (3 sprawdzenia: 16 h, przycięcie 99→23, 0 = OFF).
Mutacja `if (false && dest && …)` → 74b/c/d czerwone. Pełny pakiet: `node test3-all.js` 16.09 na Macu — WSZYSTKO PRZESZŁO (decide + macierz 672 + war-game + E2E 423 sprawdzeń + panel + zegar + składnia), exit 0.

---

## 15. 16.09 (laptop Windows) — v3.99.0: fala domykająca zostawiała statki, bez sond i LC na ekspedycjach

Owner: „od ostatniej aktualizacji bot ma problem z wysyłaniem ostatniej floty, zostają pojedyncze
sztuki w hangarze" + „wykluczyć sondy szpiegowskie i light cargo z ekspedycji".

**Co pokazał log 16.09 09:40–09:44 (liczby co do sztuki).** Fala domykająca z księżyca [2:184:1]
wpisała 508 066 993 szt. Po wysyłce w hangarze zostało `BATTLESHIP 98 876 249` i po 1 szt.
SPY_PROBE, LIGHT_CARGO, LIGHT_FIGHTER, CRUISER, BATTLE_CRUISER, DESTROYER i PLANET_BOMBER. Różnica
„wpisane − zostało" to DOKŁADNIE skład fali z 09:37 (409 190 737, wszystkie 10 typów). Z
`fleet.js` forka (`/assets/js/app/pages/fleet.js`, publiczny) wynika, że klient wysyła do
`/fleet/submitfleet` dokładnie wartości pól. **Skład wybrał więc serwer forka.** Mechanizmu nie
znamy, a kod bota w tej ścieżce nie zmienił się od v3.96.3, więc to raczej nie jest regresja bota.
Drugi objaw to już defekt bota: strona się NIE przeładowała, a `Hangar.scan()` czytało stary DOM
kroku 3 („1 113 074 331 szt., jak przed"). Bot uznał udaną wysyłkę (sloty 11/12 → 12/12) za
nieudaną, skasował wpis powrotu i dał karencję. O 09:44 spróbował drugi raz przy pełnych slotach,
co skończyło się drugim „NIE potwierdzona". Tekst „⏱Dolot—" w komunikacie pochodził z NASZEGO panelu.

**Co poszło w v3.99.0:**
1. `expo.excludeTypes` += `SPY_PROBE`, `LIGHT_CARGO` (lista jest własnością kodu, `pinCodeOwned`).
2. Udział fali: typ, którego jest mniej sztuk niż fal (`qty < left`), leci w całości z bieżącą
   falą, zamiast czekać na falę domykającą. Duże typy dzielimy w dół jak dotąd.
3. Po „Send fleet" bez adresu sukcesu hangar czytany jest ŚWIEŻO z serwera (`Hangar.scanRemote`,
   to samo ciało, bez przełączania operatora). Ekonomia sprawdza do 3 razy co 2,5–4 s, obrona do
   2 razy. Stary DOM nie jest już dowodem. Nowy stan „częściowo": wyraźny ubytek, ale mniejszy od
   wpisanego. Taką wysyłkę bot potwierdza (lot obronny nie traci wpisu i zawrotu) i daje WARN
   „gra wysłała MNIEJ, niż bot wpisał" z listą typów ponad plan (`Fly.shortfall`). Rejestr powrotów
   i `sentTotal` dostają prawdziwą liczbę. Lot obronny potwierdzony świeżym odczytem NIE zeruje
   hangaru (`noteLeftHome` zamiast `emptySourceHangar`).
4. Stempel `last_send` niesie `ships` (wpisane) i `beforeShips` (hangar przed klikiem). Na stronie
   sukcesu (`fleetSendSuccessfully`) ten sam WARN powstaje z porównania per typ. Dla lotów obronnych
   liczba w wpisie zostaje ze stempla.
5. Komunikat odmowy czyta najpierw okno sweetalert2 forka, z pominięciem `#ogx3-panel`.

**Atrapa E2E:** `staleSend` (serwer wysyła, DOM kroku 3 zostaje stary — jak 16.09; wcześniejsze
`slowRedirect` przerysowywało stronę świeżym hangarem i dlatego ten defekt był niewidoczny) oraz
`serverShips` (serwer wysyła inny skład niż w polach).

**Dowody:** decide 88a–j (+ zaktualizowane: sc. 18 „2 statki na 4 fale" = lecą od razu; 86f i
„domknięcie w JEDNYM miejscu" — nowa postać wywołań). E2E 75a–k. Mutacje (każda sprawdzona
grepem): brak świeżego odczytu → 75b/c/d/g/h/i/j/k + 88i czerwone; lista wykluczeń bez SPY/LC →
75a/d + 88a/b/c; `share` z powrotem na floor → 88d + sc. 18 (**E2E tego nie łapie**); `partial = false`
→ 75g/h/i; `noteLeftHome` wyłączone → tylko 88j (**regex; brak scenariusza E2E dla częściowej wysyłki
lotu OBRONNEGO**). Pełny pakiet: `OGX_SETTLE_MS=400 node test3-all.js` 16.09 na Windows —
WSZYSTKO PRZESZŁO (macierz 672/672, blok 77, E2E 434 sprawdzenia, panel, zegar, składnia), exit 0.

**OTWARTE / do obserwacji na żywo:**
- Czy WARN „gra wysłała MNIEJ" pojawia się regularnie i przy jakim składzie. Jeśli tak, to jest
  sprawa serwera forka (np. zgłoszenie do adminów). Bot tego nie naprawi — umie to tylko zobaczyć
  i dosłać resztę z następną falą.
- Co fork pokazuje w oknie błędu przy „Send fleet" bez przeładowania (tekst idzie teraz do logu).
- Brak E2E dla częściowej wysyłki lotu obronnego (pkt 3) — dopisać przy okazji.

---

## 16. 16.09 popołudnie (Windows) — v3.99.1: „ekspedycje lecą, choć są OFF" + szum ostrzeżenia z 3.99.0

**Zgłoszenie ownera:** „bot sam wysyła floty na ekspy, nawet jak jest OFF na ekspach". W logu: „Ekspedycje
OFF" 12:28:55, potem 24 min bez fal, „Ekspedycje ON" 12:53:00 i od 12:53:11 seria. Oba wpisy pisze
WYŁĄCZNIE obsługa przycisku `#ogx3-expo` (przełącznik), więc o 12:53:00 przycisk został „kliknięty".
Kod bota go nie klika. Zamknięte dwie drogi przełączenia bez świadomego kliknięcia (osłona na CAŁYM
panelu, `this.el` w fazie capture):
1. Klik z klawiatury (Enter/Spacja na przycisku, który po kliknięciu został w fokusie; zaufane
   zdarzenie z `detail === 0`, metoda `UI.zKlawiatury`) jest ignorowany z WARN `[PANEL]`. Przyciski
   tracą fokus po kliknięciu.
2. Karta z nieaktualnym stanem: `saveCfg` zapisuje CAŁY obiekt, więc klik w DOWOLNY przycisk albo
   zmiana pola w starej karcie przywracała po cichu przełączniki sprzed zmiany z innej karty (np.
   „Ekspedycje ON"). Przed każdym kliknięciem przycisku idzie `syncCfg()`: gdy stan się zmienił, klik
   przepada, a panel się odświeża. Przed każdą zmianą pola też `syncCfg()` (handler pola bierze
   wartość z `e.target`).
Log przełącznika mówi teraz „— kliknięcie w panelu". **Nie wiemy na pewno, co kliknęło o 12:53:00**
(możliwe: zwykłe kliknięcie ownera). Jeśli wróci, sprawdzić, czy pojawia się `[PANEL] zignorowałem`.

**Mój błąd z v3.99.0 (naprawiony):** seria fal rusza przy powrotach, więc między „przed kliknięciem"
a odczytem po wysyłce ląduje flota. Ostrzeżenie „gra wysłała MNIEJ" pisało wtedy bzdury („poleciało
~0 z 296 mln", „388 mln z 382 mln") i WPISYWAŁO tę liczbę do rejestru powrotów (83 mln zamiast
459 mln). `Fly.landedDuring`: typ, którego po wysyłce jest więcej niż przed, albo „poleciało" >
wpisane = lądowanie → bez ostrzeżenia, rejestr zostaje przy wpisanej liczbie. Granica wiedzy:
lądowanie większe niż sama wysyłka przy NIEprzeładowanej stronie wygląda jak odmowa (z samego
hangaru nie da się tego rozdzielić) — zostaje dawne „NIE potwierdzona".

**Widziane w logu, NIE ruszane:** 11:30 „sloty 11/12 (odczyt 12/12 przycięty do 11 własnych lotów
z paska)" → gra odpowiedziała „Max expedition slots exceeded" (tekst okna błędu działa od 3.99.0).
Przycinanie slotów paskiem (v3.63.0) potrafi zaniżyć zajęte sloty — do przemyślenia.

**Dowody:** panel 99.1a–f, decide 89a–d (liczby z logu 10:43/11:48/09:40), E2E 75l. Mutacje (każda
sprawdzona): klawiatura → 99.1b; brak synchronizacji przed klikiem → 99.1c/d/e; brak synchronizacji
przy zmianie pola → 99.1f; `wyladowala` ignorowane → 89d + 75l; `landedDuring` zawsze false → 89a + 75l.
Pełny pakiet: `OGX_SETTLE_MS=400 node test3-all.js` 16.09 na Windows — WSZYSTKO PRZESZŁO (macierz 672/672, blok 77, E2E 435 sprawdzeń, panel, zegar, składnia), exit 0.

---

## 17. 16.09 ~14:00 (Windows) — v3.99.2: fala domykająca z 7 statkami przy pełnym hangarze

**Zgłoszenie ownera (zrzut 13:52):** bot wysłał ekspedycję z 7 statkami (po 1 szt.), choć w hangarze
stało ~320 mln. Log: odczyt w tle 13:51:25 = 986 960 728 szt. (LC + HC + recyklery + sondy + GS, czyli
same wykluczone typy, plus 7 pojedynczych sztuk — wtedy prawda). Plan 13:52:25: „domyka serię — cały
hangar: 7 szt.". W międzyczasie wylądowała fala, a formularz o 13:52:32 wpisał `min(plan, hangar)` = po 1.
Ostatni slot ekspedycji poszedł na godzinę.

**Poprawki:**
1. Fala domykająca niesie `takeAllExcept` (lista `expo.excludeTypes`); `Fly.form` wpisuje wtedy to, co
   pokazuje formularz, poza wykluczonymi typami (`qtyFor`). Gdy formularz ma więcej niż plan, log mówi o tym
   wprost. Zwykłe fale nadal biorą `min(plan, hangar)`.
2. `expoPlan`: fala mniejsza niż 1% największej fali z tej bazy, która jest w locie (`s.expected`,
   `returnAt > now`), to „resztka" — bez wysyłki, zastój łagodny (`STALL_BENIGN`). Próg jest względny,
   więc małe floty (i atrapa E2E) bez fal w locie latają jak dotąd.

**Otwarte z tej samej sesji (sekcje 15–16, nierozstrzygnięte):** w serii 12:57–13:03 cztery fale bota
zajęły 8 slotów, a hangar zmalał o 7 udziałów zamiast 4. Bez ręcznych wysyłek w logu. Hipoteza: DRUGI BOT
(np. karta na Macu z włączonymi ekspedycjami). Pasuje też do 09:40 („serwer wysłał skład z 09:37") oraz
do „ekspedycje lecą, choć OFF". Owner ma to sprawdzić. **Do czasu wyjaśnienia nie zgłaszać adminom
„błędu serwera".**

**Dowody:** decide 90a–e, E2E 76a–d. Mutacje: `wszystko = false` → 76b/d; próg resztki wyłączony → 90a.
Pełny pakiet: `OGX_SETTLE_MS=400 node test3-all.js` 16.09 na Windows — WSZYSTKO PRZESZŁO (macierz 672/672,
blok 77, E2E 439 sprawdzeń, panel, zegar, składnia), exit 0.

---

## 18. 19.09 (Mac) — v3.100.0: transportery WRACAJĄ na ekspedycje

**Zlecenie ownera:** „chciałbym wdrożyć delikatną zmianę i zacząć wysyłać light cargo i heavy cargo na
ekspedycje, dotychczas były wyłączone z wysyłania ekspedycji".

**Co zmieniono (jedna linia kodu + testy).** `DEFAULTS.expo.excludeTypes` traci `HEAVY_CARGO` i
`LIGHT_CARGO`. Nowa lista: `ASTEROID_MINER, COLONY_SHIP, DEATH_STAR, RECYCLER, AVATAR, SPY_PROBE`.
Sondy zostają w domu (owner wykluczył je 16.09 osobno, są potrzebne do zwiadu).

**Dlaczego to bezpieczne.** Wykluczenie transporterów było DECYZJĄ WŁAŚCICIELA (v3.69.0 — HC, 07.09;
v3.99.0 — LC, 16.09), nie obejściem defektu. `expo.excludeTypes` jest czytane wyłącznie przez ścieżkę
ekspedycji (`expoPlan`, `expoHomeBody`, `takeAllExcept` fali domykającej) — Fleet Save, ratunek i mining
mają własne listy, więc nic poza ekspedycjami się nie rusza.

**Pułapka, którą trzeba było obsłużyć.** Lista jest WŁASNOŚCIĄ KODU (`pinCodeOwned`), bo `saveCfg`
zapisuje CAŁY CFG. W przeglądarce ownera leży lista v3.99.x Z transporterami — bez przypięcia
trzymałaby je w domu po każdym przeładowaniu, na zawsze, a panel nie ma pola, żeby to odkręcić.
Ten sam strażnik działał w v3.69.0 w drugą stronę. E2E sc. 58 sprawdza to teraz od nowej strony.

**Dowody:** decide 60a–e (blok odwrócony: transportery w fali, sondy i recyklery w domu; schowek nie
nadpisuje kodu) i 88a–c (regex źródła przepięty z `HEAVY_CARGO` na `SPY_PROBE`). E2E 58 (stary schowek),
75a/75d (fala 1151 szt. zamiast 601), 76a–d (typy wykluczone podmienione na RECYCLER/SPY_PROBE, żeby
scenariusz nadal badał falę domykającą z nieaktualnego planu). Mutacja `excludeTypes` z powrotem
z transporterami → 6 FAIL w decide (60a/60b/60c1/60d/88a/88b) i 4 FAIL w E2E (58×2, 75a, 75d).
Pełny pakiet: `node test3-all.js` 19.09 na Macu — WSZYSTKO PRZESZŁO (macierz 672/672, blok 77,
E2E 450 sprawdzeń, panel, zegar, składnia), exit 0.

**Do obserwacji na żywo (NIESPRAWDZONE w grze).** Hangar ownera to ~376 mln HC i ~110 mln LC, czyli
większość floty — od teraz to wszystko wychodzi na ekspedycje. Dwa skutki do popatrzenia w logu:
(1) czy WARN „gra wysłała MNIEJ, niż bot wpisał" (sekcja 15) nie robi się częstszy przy większych falach;
(2) straty na ekspedycjach obejmują teraz transportery — jeśli owner będzie chciał trzymać część w domu,
właściwym narzędziem jest `capTypes` (sufit sztuk na typ), nie powrót do wykluczenia.

---

## 19. 19.09 (Mac) — v3.101.0: rozmiar fali ekspedycji z CAŁEJ floty, nie z hangaru

**Zgłoszenie ownera:** „bot nie zawsze wysyła mniej więcej równe partie na ekspe, tutaj mamy jedną
sporo mniejszą a drugą sporo większą" + dopowiedzenie, które okazało się kluczowe: „wysłał wszystkie
floty, a jeden slot został pusty". Panel: 12 fal, sloty expo 11/12, seria 1/12.

**Co się psuło.** Rozmiar fali liczył się jako `hangar / wolne sloty`, a fala „domykająca" (warunek
`expo.used >= cap - 1`, czyli JEDEN wolny slot) brała CAŁY hangar. W stanie ustalonym wolny slot jest
zawsze jeden, więc KAŻDA fala zabierała tyle, ile zdążyło wylądować od poprzedniej wysyłki — a nie 1/N
floty. Gdy między dwiema wysyłkami wylądowały dwie fale (wysyłka to trzy kroki formularza plus odstęp
60–90 s, do tego przerwy ludzkie i alarmy obrony), szły jednym lotem i **już się nie rozdzielały**:
wracały razem, więc znowu leciały razem. Liczba lotów mogła tylko maleć, rozmiary tylko rosnąć,
a zwolniony slot zostawał pusty na stałe. Ustawienie „12 fal" działało wyłącznie przy zimnym starcie.

**Dowód przed poprawką.** Symulator na PRODUKCYJNYM `expoPlan` (konfiguracja ownera: 12 fal, 12 slotów,
flota 15,3 mld). Gdy wysyłka nadąża za powrotami, fale wychodzą równe. Gdy trwa dłużej niż odstęp między
lądowaniami, rozrzut rośnie do 4,88×, a w logu widać fale 2× większe od celu i hangar wyzerowany przy
11 lotach w powietrzu. Zasiany stan ownera (nierówne fale już w locie) przy dzisiejszym kodzie zostaje
nierówny w nieskończoność (1,73×). Skrypty: `scratchpad/sim-jitter.js`, `sim-fix.js`.

**MOJA POMYŁKA W DIAGNOZIE, żeby jej nie powtórzyć.** Najpierw „udowodniłem" mechanizm tym, że rozmiary
ze zrzutu Events są wielokrotnościami 720 765 617. To się nie broni: trzy wartości to 2u plus rosnący
ogonek, jedna to 4,03u, a suma 11 różnych wartości (16,6 mld) nie zgadza się z panelem (13,75 mld), więc
te wiersze NIE są po prostu 11 lotami w powietrzu. Owner to wychwycił („coś mi tu nie gra"). Mechanizm
obroniły dopiero kod i symulacja. Wniosek: liczby ze zrzutu to poszlaka, nie dowód.

**Co poszło w v3.101.0.** Celem jednej fali jest `flota ekspedycyjna / cap`, gdzie flota to hangar PLUS
to, co jeszcze wraca z tej bazy (`s.expected`, `returnAt > now`), a `cap = min(fale, sloty expo)`.
Cel nie zależy od tego, ile akurat wylądowało, więc nadmiar zostaje w domu na następny zwolniony slot.
- „Cały hangar" (`p.last`, niesie `takeAllExcept`) znaczy teraz WYŁĄCZNIE „hangar nie przekracza udziału
  jednej fali". Reguła „ostatni wolny slot = cały hangar" ZNIKA — to ona zlepiała fale.
- Ile fal już lata, bot bierze z DWÓCH źródeł: slotów gry i licznika serii (`inSeries`), większe z nich.
  Odczyt slotów bywa starszy niż seria, bo fale idą co 60–90 s, a strona floty nie po każdej.
- Rejestr powrotów niepełny → średnia skalowana na wszystkie zajęte sloty. Rejestr PUSTY → powrót do
  starego wzoru `hangar / wolne sloty`, czyli do zachowania sprzed tej wersji. Bot nie zaniża fal
  na podstawie niewiedzy.
- Udział jest ułamkowy (bez zaokrąglania do 1 szt.), inaczej flota mniejsza niż liczba fal tłukła się
  na pół zamiast lecieć w całości (sc. 18).
- Log zwykłej fali podaje udział, całą flotę oraz podział hangar/powietrze — po to, żeby dało się na
  żywo sprawdzić, czy fale są równe.

**Po poprawce (symulacja).** Ten sam scenariusz: 1,00× rozrzutu i 12/12 slotów zamiast 11/12, również
gdy start jest już nierówny — nierówne fale po powrocie rozchodzą się na równe SAME.

**Regresja, którą sam wprowadziłem i złapały testy.** Odprowadzanie operatora po serii
(`Fly` → `Expo.maybeReturnOperator`) było zaczepione o frazę **„domyka serię"** w opisie lotu, którą
zmieniłem w `expoPlan`. E2E sc. 36 poszło czerwone. Fraza rozpoznawcza to teraz „cały hangar"
(pilnuje jej blok „po domkniętej serii bot odprowadza operatora" w `test3-decide.js`).
Lekcja: opis lotu (`m.why`) jest tu KONTRAKTEM między modułami, nie tylko tekstem do logu.

**Dowody:** nowy blok 91a–f w `test3-decide.js` (przypadek ownera 11/12, samowyrównanie nierównych fal,
zimny start, ostatnia fala serii, dwie siatki bezpieczeństwa rejestru, dwa testy źródła). Zaktualizowane:
17e, 17b, 18, 60, 88, blok odprowadzania operatora, E2E 76a. Mutacje (każda sprawdzona): udział bez floty
w powietrzu → 91a/91a1/91b/91f; powrót reguły „ostatni wolny slot" → 91a/91a1/91e; licznik serii poza
`juzLata` → 7 testów serii; stara fraza „domyka serię" → test odprowadzania + E2E 36.
Pełny pakiet `node test3-all.js` 19.09 na Macu: macierz 672/672, E2E 450 sprawdzeń, panel, zegar,
składnia, exit 0.

**Do obserwacji na żywo.** Dzisiejsze 11 lotów jest nierównych i wyrówna się dopiero, gdy każda fala
wróci i pojedzie z nowym celem (kilka godzin). Jeśli owner chce od ręki, wystarczy wyłączyć ekspedycje,
poczekać na wszystkie powroty i włączyć — wtedy zimny start dzieli flotę na 12 równych fal.

### 19a. Jeszcze tego samego wieczoru — v3.101.1: skalowanie rejestru po slotach gry, nie po liczniku serii

Owner: „czy na pewno wszystko jest ok?". Puściłem symulator na KODZIE, KTÓRY JUŻ POSZEDŁ (sim-jitter.js
ładuje produkcyjny `expoPlan`) i wyszło, że nie: przy wysyłce wolniejszej niż lądowania rozrzut fal
wracał do 1,09–1,18×, choć miał być 1,00×.

**Przyczyna.** Gdy rejestr powrotów zna mniej lotów, niż jest w powietrzu, skalowałem średnią przez
`juzLata = max(sloty gry, licznik serii)`. Licznik serii liczy fale **WYSŁANE**, a część z nich zdążyła
już wrócić — więc mnożnik bywał za duży, flota wychodziła zawyżona, cel fali rósł i fale znowu się
rozjeżdżały. Do skalowania wolno użyć tylko liczby, którą widzi GRA (`expo.used`). Licznik serii
zostaje wyłącznie w ścieżce zapasowej (pusty rejestr), gdzie jest jedyną wiedzą, jaką bot ma.

**Pomiar (5 przebiegów, odstęp 240 s, czyli wysyłka wolniejsza od lądowań):** przed 1,09/1,18/1,09/1,18/1,18×,
po — 1,00× w każdym. Przy realnym odstępie 60–90 s: 1,00× i 32/35 wysyłek przy pełnych 12/12 slotach.

**Czego to NIE naprawia (świadome ograniczenie).** Gdy wysyłka trwa tyle samo co odstęp między
lądowaniami (240 s w teście), około jednej fali parkuje w hangarze i jeden slot bywa wolny — to limit
przepustowości (bot wysyła najwyżej jedną falę na odstęp), a nie nierówność fal. Rozmiary zostają równe.
Gdyby to doskwierało na żywo, właściwą dźwignią jest skrócenie odstępu przy zaległości w hangarze.

**Dowody:** 91g i 91g1 w `test3-decide.js` (rejestr zgodny z grą przy liczniku serii 11/12 → flota
w powietrzu 3000, nie 11 000). Mutacja `lataGra` → `juzLata`: oba czerwone. Pełny pakiet zielony
(672/672, E2E 450, exit 0). **Lekcja: pakiet testów był zielony PRZED i PO tej poprawce — złapał ją
dopiero symulator na produkcyjnym kodzie. Przy zmianach ekonomii uruchamiać obie rzeczy.**

---

## 20. 21.09 (Windows) — v3.102.0: WYCOFANIE v3.101.x, powrót do kodu v3.100.0

**Zgłoszenie (owner 21.09):** „coś chujowo wysyła ekspy w najnowszej wersji, chciałbym wrócić do
wersji 3.100, która działała bardzo dobrze".

**Co zrobione:** `git revert` commitów `a16eced` (v3.101.0), `4ce6279` (v3.101.1) i `08480e5`
(CLAUDE.md). `ogamex-3.user.js`, `test3-decide.js` i `test3-e2e.js` są identyczne z `868dd71`
(v3.100.0) poza numerem wersji. Numer to **3.102.0**, nie 3.100.0, bo Tampermonkey aktualizuje
tylko w górę. Testy bloku 91 (rozmiar fali z całej floty) zniknęły razem z kodem.

**Stan wiedzy:** sekcje 19 i 19a zostają jako historia, ale ich wniosek NIE obowiązuje. Wzór
„udział = (hangar + flota w powietrzu) / cap" wygrywał w symulatorze (rozrzut 1,00×), a na żywej
grze owner ocenił go gorzej niż stary. Logu z żywej gry z okresu 3.101.x nie analizowano — przed
kolejnym podejściem do nierównych fal najpierw zebrać log, potem zmieniać, i to za zgodą ownera.
Lekcja z sekcji 19a odwrócona: symulator też nie jest dowodem — dowodem jest żywa gra.

### 20a. Tego samego dnia — log v3.101.1 z żywej gry jednak odczytany (LevelDB Chrome'a, 09:27–10:54)

Owner po wycofaniu: „a dlaczego to było zjebane?". Log z laptopa (`ogx3_log`, `ogx3_situation`):
- 3.101.1 robiła to, co miała: 12 fal 2,78–2,81 mld szt. (rozrzut ~1,01×, poza pierwszą 2,66 mld „cały
  hangar"), sloty 12/12, rejestr powrotów kompletny (23 wpisy, `flightMs` 710 s).
- **Koszt, którego symulator nie mierzył: reszta w hangarze.** Po zapełnieniu 12. slotu (10:13:39) na
  księżycu [4:22:3] zostało ~281 mln statków ekspedycyjnych (hangar 3 088 264 183 − fala 2 807 012 683;
  potwierdza migawka hangaru z 10:41: LC 70 mln, LF 70 mln, BS 76 mln… obok 1,35 mld wykluczonych
  recyklerów) i stało ~45 min do pierwszego powrotu (10:58). To cecha wzoru, nie błąd odczytu: cel fali
  = flota/12, flota rośnie w trakcie cyklu (znaleziska), więc suma 12 fal < flota, a 13. slotu nie ma.
  Reszta nie znika w kolejnym cyklu — odtwarza się z przyrostu. Ok. 1% floty, ale WIDOCZNE (flota stoi
  przy 12/12) i wystawione na atak; owner już 16.09 zgłaszał „zostają sztuki w hangarze".
- To NAJPRAWDOPODOBNIEJ jest „chujowo wysyła" — wniosek z logu, owner jeszcze nie potwierdził.
- 3.100.0 (teraz na produkcji) nie zostawia reszty, ale ma wadę z sekcji 19: fala przy ostatnim wolnym
  slocie ALBO ostatnia w liczniku serii (`inSeries >= waves - 1`) bierze cały hangar, więc dwie fale,
  które wylądowały między wysyłkami, zlepiają się na stałe (11/12 slotów, nierówne partie).

**Propozycja (NIEWDROŻONA, czeka na decyzję ownera):** cel fali jak w 3.101 (flota/cap), ale gdy wysyłka
zajmuje OSTATNI wolny slot wg gry i hangar < 1,5× udziału — leci cały hangar. Hangar ≥ 1,5× udziału =
wylądowały dwie fale, więc leci jeden udział, a świeży odczyt slotów po wysyłce rozstrzyga resztę.
Zero reszty przy 12/12, brak trwałego zlepiania. Metryka symulatora musi liczyć TAKŻE statko-minuty
bezczynności w hangarze, nie sam rozrzut fal.

### 20b. Propozycja z 20a PRZETESTOWANA I ODRZUCONA (nie wdrażać)

Napisałem hybrydę lokalnie (udział flota/cap + „ostatni slot wg gry bierze cały hangar < 1,5 udziału"),
testy 91h–h3 + mutacja przeszły — a symulator na produkcyjnym `expoPlan` ją obalił. 30 h gry, 5 ziaren,
flota 33,4 mld, runda 40 min + 2×710 s, znaleziska ~1%/lot; metryki: rozrzut fal, śr. wolne sloty,
% floty bezczynnej przy 12/12, liczba wysłanych fal. Scenariusz „realny" (wysyłka 40 s, 10% przerw 3 min):

| wersja | rozrzut śr. | wolne sloty | bezczynne | fal/30 h |
|---|---|---|---|---|
| 3.100.0 (= 3.102.0, produkcja) | 39× | 0,56 | 0,00% | 288 |
| 3.101.1 | **1,02×** | **0,30** | 0,19% | **297** |
| hybryda 1,5× | 5,9× | 0,65 | 0,24% | 286 |
| hybryda + rejestr / bez „całego hangaru z formularza" / próg 1,2× | 4,7–5,7× | 0,40–0,64 | 0,18–0,29% | 286–295 |

Każda postać reguły „ostatni slot bierze wszystko" rozjeżdża fale i NIE poprawia przepustowości, a reszty
w hangarze realnie nie usuwa. 3.101.1 wygrywa we wszystkim poza resztą (0,2–0,3% floty średnio; w logu
~1% przez 45 min). Kod hybrydy wycofany z drzewa roboczego, nic nie commitowane. Symulator:
scratchpad sesji 21.09 (`sim.js`, ~60 linii, ładuje `expoPlan` z pliku jak `test3-decide.js`) — jest
UPROSZCZONY (jeden typ statku, brak strat, brak alarmów), więc to poszlaka, nie dowód; dowodem będzie
porównanie logów z żywej gry: 3.101.1 z 21.09 rano (12 fal 2,78–2,81 mld, 12/12) kontra doba na 3.102.0.

### 20c. v3.103.0 — PRAWDZIWA przyczyna „chujowo wysyła": „cały hangar" zlepiał dwie fale na FORMULARZU

Owner: „działaj". Druga migawka logu (do 11:45, nadal v3.101.1) pokazała to, czego w oknie 09:27–10:54 nie było:
- **11:11:16** plan `cały hangar: 2 802 119 990 szt. nie przekracza udziału jednej fali`, a wysłane
  `LIGHT_CARGO×1 405 365 977…` = **5 599 826 512 szt.** Fala „cały hangar" niesie `takeAllExcept` (v3.99.2) i
  bierze wszystko, co pokazuje formularz — a między planem a formularzem wylądowała kolejna fala.
- **11:23 → 11:44+** `resztka 37 700 952 szt. (fala w locie z tej bazy: 5 599 826 512) — nie zajmuję slotu`:
  jeden lot zajmuje miejsce dwóch, więc slot stoi pusty do powrotu podwójnej fali (~63 min). Tego dnia owner
  dostał 13. slot (11:35 „Fale ekspedycji: 13"), więc widział DWA puste sloty przy pustym hangarze.
- Czyli zlepianie usunięte w v3.101.0 z PLANU wracało przez FORMULARZ. W 3.100.0 ta sama droga działa przy
  każdej fali ostatniego slotu — wycofanie do 3.100 (v3.102.0) problemu nie usuwało, tylko go nasilało.

**Poprawka.** Plan „cały hangar" niesie `shareCtx { wPowietrzu, cap }`. `Fly` przy ładowaniu statków liczy
`udział = (stoi w formularzu + wPowietrzu) / cap`; gdy `stoi ≥ 1,5 × udział`, dzieli każdy typ przez
`stoi / udział` (typ mniejszy od dzielnika leci w całości — jak `share()` w planie), loguje powód i ustawia
`m.splitOnForm` (po takiej fali nie odprowadzamy operatora — zaraz leci następna). Nieaktualny plan z v3.99.2
(7 szt. w planie, pełny hangar w formularzu) dalej bierze wszystko, bo udział liczy się z formularza.

**Dowody:** E2E **76e–h** (w pamięci jedna fala, w grze dwie → leci 1082 BS + pojedynczy krążownik, nie 2000);
mutacja (warunek podziału na `false`) → 76f/76h czerwone, mutant wysyła 2000 = błąd z żywej gry. `test3-decide`
91i/91i1 + zaktualizowane strażniki 90e i „odprowadzanie operatora". Symulator (także 13 slotów): 3.103 = 3.101.1
lub minimalnie lepiej, 3.100 wyraźnie gorzej (rozrzut 6–60×, mniej fal).

**Zauważone, NIEnaprawione (mały koszt, brak pewności co do przyczyny):**
- 11:08:20 plan kolejnej fali w tej samej sekundzie co potwierdzenie poprzedniej: hangar sprzed wysyłki +
  rejestr już z nową falą → flota policzona 36,4 zamiast 33,6 mld, udział +8% w jednej fali. Odstęp 60–90 s
  liczy się od STARTU lotu (`burst.lastSendAt` w `Expo.send`), a formularz trwał 81 s (owner klikał równolegle).
- Licznik w opisie potrafi pokazać „fala 17/12" — `burst.sent` zeruje tylko fala „cały hangar". Kosmetyka.

**Narzędzie:** `tools/win_expo_report.py snap|report`. Na laptopie Windows zadanie Harmonogramu `OGX-expo-snap`
(co 20 min, wygasa 24.09) dopisuje log do `~/ogamex-expo-logs/log.jsonl`. `report` liczy per wersja: fale/h,
rozrzut, fale poza ±15% mediany, resztę po zajęciu ostatniego slotu, zajętość slotów z próbek.

### 20d. 22.09 rano — v3.103.0 na żywo: 13 równych fal; v3.103.1 = resztki z zaokrągleń lecą z ostatnią falą

Log ownera (zimny start 08:43, 13 fal, flota 38,86 mld): **13 fal po 2 988 882 57x, sloty 13/13** — pierwszy
w pełni równy cykl. Owner: „bot nie bierze wszystkich statków w ostatniej fali — czasem więcej, czasem pojedyncze
sztuki zostają; 3.100 z weekendu działała lepiej".
- **„Pojedyncze sztuki" (3.103.0):** 13. fala miała hangar 2 988 882 622 przy udziale 2 988 882 575 — o 47 szt.
  więcej (zaokrąglenia w dół z 12 fal). Warunek `doma <= docelowa` był fałszywy, dzielnik 1,00000002, `floor`
  zostawił po kilka sztuk z typu (52 szt.), a „resztka nie zajmuje slotu" trzymała je do powrotu. Zrzut ownera:
  12/4/12/5/11/2/13/1/1/1 w hangarze przy 13/13.
- **„Czasem więcej" (3.102 = 3.100, 21.09 popołudnie):** fale 2,09/2,24/2,24/2,24/**2,81** mld — „domyka serię —
  cały hangar" na ostatnim slocie. To NIE działało lepiej; to druga strona tej samej wady.
- **v3.103.1:** `bierzeWszystko = doma <= docelowa * 1.01`. Hangar do 1% ponad udział leci w całości; 1,03 udziału
  nadal dzielimy (91j/91j1, mutacja → oba + 91e czerwone). Symulator przy 13 slotach: identyczny rozrzut
  i liczba fal co 3.103.0 — inaczej niż progi 1,2–1,5 z 20b, bo fala nie może tą drogą urosnąć ponad 1,01 udziału.

### 20e. 22.09 ~11:30 — v3.104.0: 1,6 mld w domu przy 13/13 — rejestr zgubił falę + bot nie nadąża

Owner (zrzut 11:29): hangar 1,63 mld statków ekspedycyjnych (LC 392 mln, BS 444 mln…) przy 13/13; „w weekend
3.100 wysyłała dobrze, teraz lipa". Log 10:52–11:29 (v3.103.1) — dwie przyczyny:
1. **10:59:09 — rejestr zgubił falę.** Plan „cały hangar" z nieaktualnego odczytu (656 mln), formularz pokazał
   3 726 mln (wylądowała fala), bot wpisał całość i kliknął. W tej samej sekundzie wylądowała KOLEJNA fala
   2 989 mln — o składzie co do sztuki równym porannym falom. `Fly.shortfall` porównał hangar po wysyłce z planem
   i uznał „gra wysłała MNIEJ: 737 mln z 3 726 mln"; `landedDuring` nie łapie lądowania fali MNIEJSZEJ niż
   hangar przed kliknięciem (żaden typ nie ma „więcej niż przed"). Rejestr dostał 737 mln. **Bilans floty**:
   plan 10:10 liczył flotę na 39,2 mld, plan 11:03 — 36,1 mld. Nic nie zginęło (żadnej straty w logu), więc
   3 mld to dziura w rejestrze. Skutek: udział fali zaniżony o ~230 mln, 11 fal × 230 mln ≈ 1,6 mld w domu.
   To samo zjawisko opisuje sekcja 15 z 16.09 („poleciał co do sztuki skład fali z 09:37") — tam też
   przypisano to serwerowi forka. Hipoteza: **fork nie wysyła mniej; to zawsze lądowanie w sekundzie
   wysyłki.** Rozstrzyga lądowanie lotu z 10:59 (~12:02): hangar +3,7 mld = rejestr się mylił.
2. **11:03–11:18 — bot nie nadąża.** Odstęp 60–90 s + formularz ~20 s = 1 fala / 80–110 s, a fale lądują co
   ~80 s. Zaległość 6–7 mld (2 fale) leżała w hangarze cały cykl, sloty 11/13, na ostatnim slocie została w domu.

**v3.104.0:**
- Odstęp między falami NIE obowiązuje, gdy hangar ≥ 2 udziały i ≥ połowa slotów w locie (stan ustalony); zimny
  start zachowuje rytm. Sim (13 slotów): rozrzut bez zmian, mniej floty w domu, więcej fal. Testy 91k–k2.
- Bilans floty: `burst.flota` = flota z poprzedniego planu; spadek ≥ 5% → WARN „rejestr zgubił falę / strata
  na ekspedycji, sprawdź listę ruchów" raz na 30 min. Testy 91l–l2.
- Rejestr (punkt 1) — decyzja po dowodzie z 12:02, patrz 20f.
Pełny pakiet: 672/672, E2E 454, exit 0. Warianty odrzucone w sim: „ostatni slot bierze hangar < 1,5 udziału"
(rozrzut 1,7–4,5×), także w połączeniu z brakiem odstępu (4×).

### 20f. 22.09 12:04 — DOWÓD: fork wysłał wszystko, rejestr się mylił → rejestr = to, co wpisano

Log ownera: 12:03:50 plan „cały hangar 1 635 mln" (sloty 12/13), 12:04:53 formularz **8 276 mln** — w 63 s
wylądowało +6,64 mld = dwie fale: lot z 10:59:09 (3 726 mln + znaleziska) i lot z 10:59:51 (2 989 mln + znaleziska).
Gdyby lot z 10:59:09 miał 737 mln, hangar miałby 5,4 mld. Bilans floty w planie: 42,0 mld (z „36,1"). **Fork nie
wysyła mniej — v3.99.0 myliło lądowanie w sekundzie wysyłki z częściową wysyłką**, a `landedDuring` nie widzi
lądowania fali MNIEJSZEJ od hangaru sprzed kliknięcia. Przy okazji: podział „całego hangaru" na formularzu
z v3.103.0 zadziałał na żywo (2,56 udziału → dwie fale po 3,23 mld zamiast jednej 8,3 mld).

**v3.104.0 (część 2):** `Fly.confirmed` wpisuje do rejestru powrotów `last_send.total` (liczby z pól formularza po
ponownym odczycie), nie `sentReal` z odejmowania hangaru. Log po wysyłce mówi „po wysyłce w hangarze zostało
ponad plan … to lądowanie fali w sekundzie wysyłki" (info, nie warn). E2E: 75g/75i odwrócone (harness dalej
symuluje „mniej", żeby pilnować, że bot nie traktuje tego jak odmowy), nowy 75m–o odtwarza 10:59 co do liczb
(3726 wpisane, 2989 ląduje → rejestr 3726); mutacja (stary `e0.total = sentReal`) → 75i/75n czerwone.
Jeśli fork KIEDYŚ naprawdę wyśle mniej: rejestr zawyży flotę w powietrzu o różnicę, udział będzie za duży
o różnica/13 i hangar opróżni się przed ostatnim slotem — slot wolny do najbliższego powrotu (minuty), a bilans
floty w kolejnym planie to pokaże. Koszt tamtego błędu był większy (4% floty w domu przez godzinę, co cykl).

### 20g. 22.09 15:01 — v3.105.0: OSTATNI wolny slot bierze CAŁY hangar, bez progu (decyzja ownera)

Owner (zrzut 15:01, v3.104.0, 13/13): w hangarze 188 mln statków ekspedycyjnych (LC 15 mln, LF 48 mln, BS 53 mln…)
— „nic się nie zmieniło! ostatnia fala powinna wysyłać wszystkie statki, które zostały!". Log 14:59:37: fala 13/13,
hangar 3 113 mln przy udziale 2 925 mln (1,06 — znaleziska z całego cyklu), tolerancja 1% za mała → 188 mln
w domu (0,5% floty). Rano było 1,6 mld (4%), więc v3.104 pomogła, ale owner nie chce ŻADNEJ floty w domu przy
pełnych slotach. **Priorytet ownera: pełne sloty i pusty hangar > równość fal.** Zapisać, nie dyskutować.

**v3.105.0:** `bierzeWszystko = doma <= docelowa * 1.01 || ostatniSlot`, gdzie `ostatniSlot = expo.used >= cap - 1`
(z GRY, przycięte paskiem; licznik serii bez głosu — 91e4). Na ostatnim slocie także formularz nie dzieli
(`shareCtx.lastSlot` → `Fly` bierze wszystko, 76ter). Przy ≥ 2 wolnych slotach nadal udział z całej floty i podział
na formularzu (76bis, 91e3, 91a1). Testy 91a/91a1 z 19.09 ODWRÓCONE (opisywały poprzednią prośbę ownera).
Mutacja (bez `|| ostatniSlot`) → 91a/91e/91e1/91e2 czerwone.

**Cena wg symulatora (13 slotów, 30 h, 5 ziaren; poszlaka, nie dowód):** flota w domu przy 13/13 = 0,00% (cel),
ale rozrzut fal 2–27× (dwie fale, które wylądowały przed ostatnim slotem, lecą jednym lotem i wracają razem),
śr. wolnych slotów 0,85–1,25 (v3.104: 0,15–0,99) i ~6% mniej wysłanych fal na 30 h. Alternatywa niewdrożona:
v3.104 z tolerancją 10% zamiast 1% (pokryłaby dzisiejsze 1,06, nie pokryłaby dwóch fal na ostatnim slocie).
Owner dostał te liczby; decyzja jego.
